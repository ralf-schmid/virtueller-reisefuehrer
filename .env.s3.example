# S3-Konfiguration für virtuelle Stadtführungen
# Kopieren Sie diese Datei zu .env.s3 und passen Sie die Werte an

# S3 Bucket Name
S3_BUCKET=your-bucket-name

# AWS Credentials
AWS_ACCESS_KEY=your-access-key-id
AWS_SECRET_KEY=your-secret-access-key

# Optional: AWS Region (default: wird aus Bucket ermittelt)
# AWS_REGION=eu-central-1

# Optional: S3-Mount Optionen
# S3FS_OPTIONS="-o allow_other -o use_cache=/tmp/s3fs-cache"

# Verwendung:
# 1. Kopieren: cp .env.s3.example .env.s3
# 2. Anpassen: nano .env.s3
# 3. Laden: source .env.s3 && ./setup-s3.sh
